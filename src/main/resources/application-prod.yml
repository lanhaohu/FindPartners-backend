spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/FindPartners
    username: root
    password: root
  #session失效时间
  session:
    timeout: 86400